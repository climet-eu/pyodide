diff --git a/codecs/frontend/src/loader.rs b/codecs/frontend/src/loader.rs
index db45e0ee..c3ae00b8 100644
--- a/codecs/frontend/src/loader.rs
+++ b/codecs/frontend/src/loader.rs
@@ -97,7 +97,20 @@ impl WasmCodecClassLoader {
     where
         Store<(), ValidatedEngine<E>>: Send + Sync,
     {
-        let wasm_component = std::fs::read(path)
+        struct FileNoSize(std::fs::File);
+
+        impl std::io::Read for FileNoSize {
+            fn read(&mut self, buf: &mut [u8]) -> std::io::Result<usize> {
+                self.0.read(buf)
+            }
+        }
+
+        let file = std::fs::File::open(path)
+            .map_err(|err| WasmCodecLoaderError::ReadWasmBinaryFile { source: err })?;
+        let mut file = FileNoSize(file);
+
+        let mut wasm_component = Vec::new();
+        std::io::Read::read_to_end(&mut file, &mut wasm_component)
             .map_err(|err| WasmCodecLoaderError::ReadWasmBinaryFile { source: err })?;
 
         Self::new_type(wasm_component, engine)
