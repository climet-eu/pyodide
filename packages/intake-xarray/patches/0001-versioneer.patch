diff --git a/setup.py b/setup.py
index 94958a6..ddb2f96 100644
--- a/setup.py
+++ b/setup.py
@@ -14,7 +14,7 @@ INSTALL_REQUIRES = ['intake >=2', 'xarray >=02022', 'zarr', 'dask >=2.2', 'netcd
 
 setup(
     name='intake-xarray',
-    version=versioneer.get_version(),
+    version='0.7.0',
     cmdclass=versioneer.get_cmdclass(),
     description='xarray plugins for Intake',
     url='https://github.com/intake/intake-xarray',
diff --git a/versioneer.py b/versioneer.py
index 9f8880b..3793a0e 100644
--- a/versioneer.py
+++ b/versioneer.py
@@ -339,9 +339,9 @@ def get_config_from_root(root):
     # configparser.NoOptionError (if it lacks "VCS="). See the docstring at
     # the top of versioneer.py for instructions on writing your setup.cfg .
     setup_cfg = os.path.join(root, "setup.cfg")
-    parser = configparser.SafeConfigParser()
+    parser = configparser.ConfigParser()
     with open(setup_cfg, "r") as f:
-        parser.readfp(f)
+        parser.read_file(f)
     VCS = parser.get("versioneer", "VCS")  # mandatory
 
     def get(parser, name):
